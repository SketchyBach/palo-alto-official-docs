---
url: https://docs.paloaltonetworks.com/ngfw/pan-os-cli-quick-start/cli-changes/new-show-commands-12-2
fetched_at: 2026-08-13T16:55:24Z
source: palo-alto-main
---

# Show Commands Introduced in PAN-OS 12.2 Clear

Show Commands Introduced in PAN-OS 12.2 

 Home 

 EN

 Location 

 Documentation Home 

 Palo Alto Networks 

 Support 

 Live Community 

 Knowledge Base 

 >

 Strata Copilot

 Show Commands Introduced in PAN-OS 12.2 

 Updated on 

 Aug 28, 2025 

 Focus 

 Download PDF 

 Filter

 Expand All 
 | 
 Collapse All 

 Next-Generation Firewall Docs 

 Getting Started 

 Administration 

 Networking 

 Quick Start 

 Reference 

 Incidents & Alerts 

 Release Notes 

 Select a Document 

 PAN-OS 12.2 

 PAN-OS 12.1 

 PAN-OS 11.2 

 PAN-OS 11.1 

 PAN-OS 11.0 (EoL) 

 PAN-OS 10.2 

 PAN-OS 10.1 (EoL) 

 PAN-OS 10.0 (EoL) 

 PAN-OS 9.1 (EoL) 

 PAN-OS 9.0 (EoL) 

 PAN-OS 8.1 (EoL) 

 Help 

 Select a Document 

 PAN-OS 12.2 

 PAN-OS 12.1 

 PAN-OS 11.2 

 PAN-OS 11.1 

 PAN-OS 10.2 

 PAN-OS 10.1 

 New Features 

 Updated on 

 Aug 28, 2025 

 Focus 

 Home 

 Next-Generation Firewall 

 CLI Changes in PAN-OS 11.1+ 

 Show Commands Introduced in PAN-OS 12.2 

 Download PDF 

 Next-Generation Firewall 

 Show Commands Introduced in PAN-OS 12.2 

 Table of Contents 

 Filter

 Expand All 
 | 
 Collapse All 

 Next-Generation Firewall Docs 

 Getting Started 

 Administration 

 Networking 

 Quick Start 

 Reference 

 Incidents & Alerts 

 Release Notes 

 Select a Document 

 PAN-OS 12.2 

 PAN-OS 12.1 

 PAN-OS 11.2 

 PAN-OS 11.1 

 PAN-OS 11.0 (EoL) 

 PAN-OS 10.2 

 PAN-OS 10.1 (EoL) 

 PAN-OS 10.0 (EoL) 

 PAN-OS 9.1 (EoL) 

 PAN-OS 9.0 (EoL) 

 PAN-OS 8.1 (EoL) 

 Help 

 Select a Document 

 PAN-OS 12.2 

 PAN-OS 12.1 

 PAN-OS 11.2 

 PAN-OS 11.1 

 PAN-OS 10.2 

 PAN-OS 10.1 

 New Features 

 Previous 

 Load Commands Removed in PAN-OS 12.1 

 Next 

 Set Commands Introduced in PAN-OS 12.2 

 Show Commands Introduced in PAN-OS 12.2 

 Command line interface 'show' commands that are new in PAN-OS 12.2: 

 The following commands are new in the 12.2 release. 

 show device-group <name> device-context-segment
show device-group <name> device-context-segment <name>
show device-group <name> metadata-profile
show device-group <name> metadata-profile <name>
show device-group <name> mobile-network
show device-group <name> mobile-network equipment
show device-group <name> mobile-network equipment <name>
show device-group <name> mobile-network equipment-group
show device-group <name> mobile-network equipment-group <name>
show device-group <name> mobile-network subscriber
show device-group <name> mobile-network subscriber <name>
show device-group <name> mobile-network subscriber-group
show device-group <name> mobile-network subscriber-group <name>
show device-group <name> post-rulebase decryption rules <name> type quic-forward-proxy
show device-group <name> post-rulebase decryption rules <name> type quic-inbound-inspection
show device-group <name> pre-rulebase decryption rules <name> type quic-forward-proxy
show device-group <name> pre-rulebase decryption rules <name> type quic-inbound-inspection
show device-group <name> profiles ip-defense
show device-group <name> profiles ip-defense <name>
show device-group <name> profiles ip-defense <name> edl
show device-group <name> profiles ip-defense <name> edl <name>
show device-group <name> profiles ip-defense <name> edl <name> scope
show device-group <name> profiles ip-defense <name> edl <name> scope all-packets
show device-group <name> profiles ip-defense <name> edl <name> scope no-dns
show device-group <name> profiles ip-defense <name> edl <name> scope no-dns edl-type
show device-group <name> profiles ip-defense <name> edl <name> scope no-dns edl-type ip-address
show device-group <name> profiles ip-defense <name> edl <name> scope no-dns edl-type ip-and-port
show device-group <name> profiles ip-defense <name> rule
show device-group <name> profiles ip-defense <name> rule <name>
show device-group <name> profiles ip-defense <name> rule <name> match
show device-group <name> profiles ip-defense <name> rule <name> match and-condition
show device-group <name> profiles ip-defense <name> rule <name> match and-condition <name>
show device-group <name> profiles ip-defense <name> rule <name> match and-condition <name> category
show device-group <name> profiles ip-defense <name> rule <name> match and-condition <name> category <name>
show device-group <name> profiles ip-defense <name> rule <name> match and-condition <name> category <name> not-tag
show device-group <name> profiles ip-defense <name> rule <name> match and-condition <name> category <name> not-tag <name>
show device-group <name> profiles ip-defense <name> rule <name> match and-condition <name> category <name> tag
show device-group <name> profiles ip-defense <name> rule <name> match and-condition <name> category <name> tag <name>
show device-group <name> profiles ip-defense <name> rule <name> match and-condition <name> category <name> variable
show device-group <name> profiles ip-defense <name> rule <name> match and-condition <name> category <name> variable <name>
show device-group <name> profiles ip-defense <name> rule <name> match and-condition <name> not-category
show device-group <name> profiles ip-defense <name> rule <name> match and-condition <name> not-category <name>
show device-group <name> profiles ip-defense <name> rule <name> match and-condition <name> not-category <name> tag
show device-group <name> profiles ip-defense <name> rule <name> match and-condition <name> not-category <name> tag <name>
show device-group <name> profiles ip-defense <name> rule <name> match and-condition <name> not-category <name> variable
show device-group <name> profiles ip-defense <name> rule <name> match and-condition <name> not-category <name> variable <name>
show device-group <name> profiles ip-defense <name> rule <name> match any
show device-group <name> profiles ip-defense <name> rule <name> match unkown
show device-group <name> profiles sdwan-path-quality <name> metric bandwidth
show device-group <name> profiles spyware <name> botnet-domains blocklist
show device-group <name> profiles spyware <name> botnet-domains blocklist <name>
show device-group <name> profiles spyware <name> botnet-domains lists <name> action bypass
show device-group <name> profiles url-filtering <name> http-header-logging
show device-group <name> profiles url-filtering <name> http-header-logging type
show device-group <name> profiles url-filtering <name> http-header-logging type request-headers
show device-group <name> profiles url-filtering <name> http-header-logging type request-headers <name>
show device-group <name> profiles url-filtering <name> http-header-logging type response-headers
show device-group <name> profiles url-filtering <name> http-header-logging type response-headers <name>
show device-group <name> threats vulnerability <name> default-action exclude-inline-cloud-analysis-and-ldl
show deviceconfig plugins
show deviceconfig plugins dlp
show deviceconfig plugins dlp config
show deviceconfig plugins dlp config scan-report
show deviceconfig plugins dlp config scan-report snippet
show deviceconfig setting cloud-gp-svc
show deviceconfig setting cloud-userid discovery-service
show deviceconfig setting iot metadata-collection
show log-collector <name> accounting-setting
show log-collector <name> accounting-setting admin-lockout
show log-collector <name> accounting-setting admin-session
show log-collector <name> accounting-setting api
show log-collector <name> accounting-setting api key
show log-collector <name> accounting-setting users
show log-collector <name> accounting-setting users <name>
show log-collector <name> accounting-setting users <name> permissions
show log-collector <name> accounting-setting users <name> permissions role-based
show shared admin-role <name> role device-group webui objects mobile-network
show shared admin-role <name> role device-group webui objects panos-shield
show shared admin-role <name> role panorama webui objects mobile-network
show shared admin-role <name> role panorama webui objects panos-shield
show shared device-context-segment
show shared device-context-segment <name>
show shared metadata-profile
show shared metadata-profile <name>
show shared mobile-network
show shared mobile-network equipment
show shared mobile-network equipment <name>
show shared mobile-network equipment-group
show shared mobile-network equipment-group <name>
show shared mobile-network subscriber
show shared mobile-network subscriber <name>
show shared mobile-network subscriber-group
show shared mobile-network subscriber-group <name>
show shared panos-shield-override
show shared panos-shield-override profiles
show shared panos-shield-override profiles vulnerability
show shared panos-shield-override profiles vulnerability <name>
show shared panos-shield-override profiles vulnerability <name> threat-exception
show shared panos-shield-override profiles vulnerability <name> threat-exception <name>
show shared panos-shield-override profiles vulnerability <name> threat-exception <name> action
show shared panos-shield-override profiles vulnerability <name> threat-exception <name> action alert
show shared panos-shield-override profiles vulnerability <name> threat-exception <name> action allow
show shared panos-shield-override profiles vulnerability <name> threat-exception <name> action block-ip
show shared panos-shield-override profiles vulnerability <name> threat-exception <name> action default
show shared panos-shield-override profiles vulnerability <name> threat-exception <name> action drop
show shared panos-shield-override profiles vulnerability <name> threat-exception <name> action reset-both
show shared panos-shield-override profiles vulnerability <name> threat-exception <name> action reset-client
show shared panos-shield-override profiles vulnerability <name> threat-exception <name> action reset-server
show shared panos-shield-override profiles vulnerability <name> threat-exception <name> exempt-ip
show shared panos-shield-override profiles vulnerability <name> threat-exception <name> exempt-ip <name>
show shared panos-shield-override profiles vulnerability <name> threat-exception <name> time-attribute
show shared post-rulebase decryption rules <name> type quic-forward-proxy
show shared post-rulebase decryption rules <name> type quic-inbound-inspection
show shared pre-rulebase decryption rules <name> type quic-forward-proxy
show shared pre-rulebase decryption rules <name> type quic-inbound-inspection
show shared profiles ip-defense
show shared profiles ip-defense <name>
show shared profiles ip-defense <name> edl
show shared profiles ip-defense <name> edl <name>
show shared profiles ip-defense <name> edl <name> scope
show shared profiles ip-defense <name> edl <name> scope all-packets
show shared profiles ip-defense <name> edl <name> scope no-dns
show shared profiles ip-defense <name> edl <name> scope no-dns edl-type
show shared profiles ip-defense <name> edl <name> scope no-dns edl-type ip-address
show shared profiles ip-defense <name> edl <name> scope no-dns edl-type ip-and-port
show shared profiles ip-defense <name> rule
show shared profiles ip-defense <name> rule <name>
show shared profiles ip-defense <name> rule <name> match
show shared profiles ip-defense <name> rule <name> match and-condition
show shared profiles ip-defense <name> rule <name> match and-condition <name>
show shared profiles ip-defense <name> rule <name> match and-condition <name> category
show shared profiles ip-defense <name> rule <name> match and-condition <name> category <name>
show shared profiles ip-defense <name> rule <name> match and-condition <name> category <name> not-tag
show shared profiles ip-defense <name> rule <name> match and-condition <name> category <name> not-tag <name>
show shared profiles ip-defense <name> rule <name> match and-condition <name> category <name> tag
show shared profiles ip-defense <name> rule <name> match and-condition <name> category <name> tag <name>
show shared profiles ip-defense <name> rule <name> match and-condition <name> category <name> variable
show shared profiles ip-defense <name> rule <name> match and-condition <name> category <name> variable <name>
show shared profiles ip-defense <name> rule <name> match and-condition <name> not-category
show shared profiles ip-defense <name> rule <name> match and-condition <name> not-category <name>
show shared profiles ip-defense <name> rule <name> match and-condition <name> not-category <name> tag
show shared profiles ip-defense <name> rule <name> match and-condition <name> not-category <name> tag <name>
show shared profiles ip-defense <name> rule <name> match and-condition <name> not-category <name> variable
show shared profiles ip-defense <name> rule <name> match and-condition <name> not-category <name> variable <name>
show shared profiles ip-defense <name> rule <name> match any
show shared profiles ip-defense <name> rule <name> match unkown
show shared profiles sdwan-path-quality <name> metric bandwidth
show shared profiles spyware <name> botnet-domains blocklist
show shared profiles spyware <name> botnet-domains blocklist <name>
show shared profiles spyware <name> botnet-domains lists <name> action bypass
show shared profiles url-filtering <name> http-header-logging
show shared profiles url-filtering <name> http-header-logging type
show shared profiles url-filtering <name> http-header-logging type request-headers
show shared profiles url-filtering <name> http-header-logging type request-headers <name>
show shared profiles url-filtering <name> http-header-logging type response-headers
show shared profiles url-filtering <name> http-header-logging type response-headers <name>
show shared threats vulnerability <name> default-action exclude-inline-cloud-analysis-and-ldl
show template <name> config deviceconfig setting cloud-gp-svc
show template <name> config deviceconfig setting cloud-userid discovery-service
show template <name> config deviceconfig setting conn-pool-settings
show template <name> config deviceconfig setting conn-pool-settings conn-pool
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name>
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> conn-settings
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> conn-settings params
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> conn-settings params <name>
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> conn-settings params <name> value
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> conn-settings params <name> value value-str-arr
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> conn-settings params <name> value value-str-arr <name>
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> general-settings
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> general-settings <name>
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> general-settings <name> value
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> general-settings <name> value value-str-arr
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> general-settings <name> value value-str-arr <name>
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> pqc-settings
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> pqc-settings params
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> pqc-settings params <name>
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> pqc-settings params <name> value
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> pqc-settings params <name> value value-str-arr
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> pqc-settings params <name> value value-str-arr <name>
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> tcp-settings
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> tcp-settings params
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> tcp-settings params <name>
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> tcp-settings params <name> value
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> tcp-settings params <name> value value-str-arr
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> tcp-settings params <name> value value-str-arr <name>
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> tls-settings
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> tls-settings params
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> tls-settings params <name>
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> tls-settings params <name> value
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> tls-settings params <name> value value-str-arr
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> tls-settings params <name> value value-str-arr <name>
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> tls-settings tls-version
show template <name> config deviceconfig setting conn-pool-settings conn-pool <name> tls-settings tls-version <name>
show template <name> config deviceconfig setting iot metadata-collection
show template <name> config deviceconfig setting ip-defense
show template <name> config deviceconfig system usb-port
show template <name> config network dhcp interface <name> relay ip overwrite
show template <name> config network interface aggregate-ethernet <name> layer2-switch
show template <name> config network interface aggregate-ethernet <name> layer2-switch lacp
show template <name> config network interface aggregate-ethernet <name> layer2-switch lacp high-availability
show template <name> config network interface aggregate-ethernet <name> layer2-switch lacp multi-chassis-lacp-monitoring
show template <name> config network interface aggregate-ethernet <name> layer2-switch lldp
show template <name> config network interface aggregate-ethernet <name> layer2-switch lldp high-availability
show template <name> config network interface aggregate-ethernet <name> layer2-switch storm-control
show template <name> config network interface aggregate-ethernet <name> layer2-switch storm-control broadcast
show template <name> config network interface aggregate-ethernet <name> layer2-switch storm-control multicast
show template <name> config network interface aggregate-ethernet <name> layer2-switch storm-control unicast
show template <name> config network interface aggregate-ethernet <name> layer2-switch type
show template <name> config network interface aggregate-ethernet <name> layer2-switch type access
show template <name> config network interface aggregate-ethernet <name> layer2-switch type trunk
show template <name> config network interface aggregate-ethernet <name> layer2-switch type trunk native
show template <name> config network interface aggregate-ethernet <name> layer2-switch type trunk units
show template <name> config network interface aggregate-ethernet <name> layer2-switch type trunk units <name>
show template <name> config network interface aggregate-ethernet <name> layer3 proxy-arp
show template <name> config network interface aggregate-ethernet <name> layer3 proxy-arp address
show template <name> config network interface aggregate-ethernet <name> layer3 proxy-arp address <name>
show template <name> config network interface aggregate-ethernet <name> layer3 units <name> proxy-arp
show template <name> config network interface aggregate-ethernet <name> layer3 units <name> proxy-arp address
show template <name> config network interface aggregate-ethernet <name> layer3 units <name> proxy-arp address <name>
show template <name> config network interface cellular <name> layer3 units
show template <name> config network interface cellular <name> layer3 units <name>
show template <name> config network interface cellular <name> layer3 units <name> adjust-tcp-mss
show template <name> config network interface cellular <name> layer3 units <name> ddns-config
show template <name> config network interface cellular <name> layer3 units <name> ddns-config ddns-vendor-config
show template <name> config network interface cellular <name> layer3 units <name> ddns-config ddns-vendor-config <name>
show template <name> config network interface cellular <name> layer3 units <name> ipv6
show template <name> config network interface cellular <name> layer3 units <name> sdwan-link-settings
show template <name> config network interface cellular <name> layer3 units <name> sdwan-link-settings upstream-nat
show template <name> config network interface cellular <name> layer3 units <name> sdwan-link-settings upstream-nat ddns
show template <name> config network interface cellular <name> layer3 units <name> sdwan-link-settings upstream-nat static-ip
show template <name> config network interface cellular <name> layer3 units <name> sim
show template <name> config network interface cellular <name> layer3 units <name> sim slot
show template <name> config network interface cellular <name> layer3 units <name> sim slot <name>
show template <name> config network interface cluster-ethernet <name> layer3 proxy-arp
show template <name> config network interface cluster-ethernet <name> layer3 proxy-arp address
show template <name> config network interface cluster-ethernet <name> layer3 proxy-arp address <name>
show template <name> config network interface cluster-ethernet <name> layer3 units <name> proxy-arp
show template <name> config network interface cluster-ethernet <name> layer3 units <name> proxy-arp address
show template <name> config network interface cluster-ethernet <name> layer3 units <name> proxy-arp address <name>
show template <name> config network interface ethernet <name> layer2-switch
show template <name> config network interface ethernet <name> layer2-switch lldp
show template <name> config network interface ethernet <name> layer2-switch lldp high-availability
show template <name> config network interface ethernet <name> layer2-switch storm-control
show template <name> config network interface ethernet <name> layer2-switch storm-control broadcast
show template <name> config network interface ethernet <name> layer2-switch storm-control multicast
show template <name> config network interface ethernet <name> layer2-switch storm-control unicast
show template <name> config network interface ethernet <name> layer2-switch type
show template <name> config network interface ethernet <name> layer2-switch type access
show template <name> config network interface ethernet <name> layer2-switch type trunk
show template <name> config network interface ethernet <name> layer2-switch type trunk native
show template <name> config network interface ethernet <name> layer2-switch type trunk units
show template <name> config network interface ethernet <name> layer2-switch type trunk units <name>
show template <name> config network interface ethernet <name> layer3 proxy-arp
show template <name> config network interface ethernet <name> layer3 proxy-arp address
show template <name> config network interface ethernet <name> layer3 proxy-arp address <name>
show template <name> config network interface ethernet <name> layer3 units <name> proxy-arp
show template <name> config network interface ethernet <name> layer3 units <name> proxy-arp address
show template <name> config network interface ethernet <name> layer3 units <name> proxy-arp address <name>
show template <name> config network interface fail-open
show template <name> config network interface fail-open <name>
show template <name> config network interface vlan proxy-arp
show template <name> config network interface vlan proxy-arp address
show template <name> config network interface vlan proxy-arp address <name>
show template <name> config network interface vlan units <name> proxy-arp
show template <name> config network interface vlan units <name> proxy-arp address
show template <name> config network interface vlan units <name> proxy-arp address <name>
show template <name> config network profiles apn-profile
show template <name> config network profiles apn-profile <name>
show template <name> config network profiles stp-profile
show template <name> config network profiles stp-profile <name>
show template <name> config network profiles stp-profile <name> global-parameters
show template <name> config network profiles stp-profile <name> instance-parameters
show template <name> config network profiles stp-profile <name> port-parameters
show template <name> config network profiles stp-profile <name> port-parameters <name>
show template <name> config network routing-profile multicast mld-interface-query-profile
show template <name> config network routing-profile multicast mld-interface-query-profile <name>
show template <name> config network tunnel global-protect-gateway <name> ikev2
show template <name> config network tunnel global-protect-gateway <name> ikev2 Authentication
show template <name> config network tunnel global-protect-gateway <name> ikev2 Authentication certificate
show template <name> config network tunnel global-protect-gateway <name> ikev2 Authentication master-key
show template <name> config shared admin-role <name> role device webui objects mobile-network
show template <name> config shared admin-role <name> role device webui objects panos-shield
show template <name> config shared admin-role <name> role vsys webui objects mobile-network
show template <name> config shared admin-role <name> role vsys webui objects panos-shield
show template <name> config shared dlp-settings
show template <name> config shared non-file-based-dlp-settings
show template <name> config vsys <name> device-context-segment-association
show template <name> config vsys <name> device-context-segment-association <name>
show template <name> config vsys <name> global-protect global-protect-gateway <name> cloud-gp-svc
show template <name> config vsys <name> global-protect global-protect-gateway <name> cloud-gp-svc params
show template <name> config vsys <name> global-protect global-protect-gateway <name> cloud-gp-svc params <name>
show template <name> config vsys <name> global-protect global-protect-gateway <name> cloud-gp-svc params <name> value
show template <name> config vsys <name> global-protect global-protect-gateway <name> cloud-gp-svc params <name> value value-str-arr
show template <name> config vsys <name> global-protect global-protect-gateway <name> cloud-gp-svc params <name> value value-str-arr <name>
show template <name> config vsys <name> global-protect global-protect-gateway <name> cloud-gp-svc public-address
show template <name> config vsys <name> global-protect global-protect-portal <name> portal-config cloud-gp-svc
show template <name> config vsys <name> global-protect global-protect-portal <name> portal-config cloud-gp-svc params
show template <name> config vsys <name> global-protect global-protect-portal <name> portal-config cloud-gp-svc params <name>
show template <name> config vsys <name> global-protect global-protect-portal <name> portal-config cloud-gp-svc params <name> value
show template <name> config vsys <name> global-protect global-protect-portal <name> portal-config cloud-gp-svc params <name> value value-str-arr
show template <name> config vsys <name> global-protect global-protect-portal <name> portal-config cloud-gp-svc params <name> value value-str-arr <name>
show template <name> config vsys <name> setting session
show template-stack <name> config devices <name> deviceconfig setting cloud-gp-svc
show template-stack <name> config devices <name> deviceconfig setting cloud-userid discovery-service
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name>
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> conn-settings
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> conn-settings params
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> conn-settings params <name>
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> conn-settings params <name> value
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> conn-settings params <name> value value-str-arr
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> conn-settings params <name> value value-str-arr <name>
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> general-settings
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> general-settings <name>
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> general-settings <name> value
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> general-settings <name> value value-str-arr
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> general-settings <name> value value-str-arr <name>
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> pqc-settings
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> pqc-settings params
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> pqc-settings params <name>
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> pqc-settings params <name> value
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> pqc-settings params <name> value value-str-arr
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> pqc-settings params <name> value value-str-arr <name>
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> tcp-settings
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> tcp-settings params
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> tcp-settings params <name>
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> tcp-settings params <name> value
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> tcp-settings params <name> value value-str-arr
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> tcp-settings params <name> value value-str-arr <name>
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> tls-settings
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> tls-settings params
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> tls-settings params <name>
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> tls-settings params <name> value
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> tls-settings params <name> value value-str-arr
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> tls-settings params <name> value value-str-arr <name>
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> tls-settings tls-version
show template-stack <name> config devices <name> deviceconfig setting conn-pool-settings conn-pool <name> tls-settings tls-version <name>
show template-stack <name> config devices <name> deviceconfig setting iot metadata-collection
show template-stack <name> config devices <name> deviceconfig setting ip-defense
show template-stack <name> config devices <name> deviceconfig system usb-port
show template-stack <name> config devices <name> network dhcp interface <name> relay ip overwrite
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer2-switch
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer2-switch lacp
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer2-switch lacp high-availability
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer2-switch lacp multi-chassis-lacp-monitoring
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer2-switch lldp
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer2-switch lldp high-availability
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer2-switch storm-control
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer2-switch storm-control broadcast
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer2-switch storm-control multicast
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer2-switch storm-control unicast
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer2-switch type
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer2-switch type access
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer2-switch type trunk
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer2-switch type trunk native
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer2-switch type trunk units
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer2-switch type trunk units <name>
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer3 proxy-arp
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer3 proxy-arp address
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer3 proxy-arp address <name>
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer3 units <name> proxy-arp
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer3 units <name> proxy-arp address
show template-stack <name> config devices <name> network interface aggregate-ethernet <name> layer3 units <name> proxy-arp address <name>
show template-stack <name> config devices <name> network interface cellular <name> layer3 units
show template-stack <name> config devices <name> network interface cellular <name> layer3 units <name>
show template-stack <name> config devices <name> network interface cellular <name> layer3 units <name> adjust-tcp-mss
show template-stack <name> config devices <name> network interface cellular <name> layer3 units <name> ddns-config
show template-stack <name> config devices <name> network interface cellular <name> layer3 units <name> ddns-config ddns-vendor-config
show template-stack <name> config devices <name> network interface cellular <name> layer3 units <name> ddns-config ddns-vendor-config <name>
show template-stack <name> config devices <name> network interface cellular <name> layer3 units <name> ipv6
show template-stack <name> config devices <name> network interface cellular <name> layer3 units <name> sdwan-link-settings
show template-stack <name> config devices <name> network interface cellular <name> layer3 units <name> sdwan-link-settings upstream-nat
show template-stack <name> config devices <name> network interface cellular <name> layer3 units <name> sdwan-link-settings upstream-nat ddns
show template-stack <name> config devices <name> network interface cellular <name> layer3 units <name> sdwan-link-settings upstream-nat static-ip
show template-stack <name> config devices <name> network interface cellular <name> layer3 units <name> sim
show template-stack <name> config devices <name> network interface cellular <name> layer3 units <name> sim slot
show template-stack <name> config devices <name> network interface cellular <name> layer3 units <name> sim slot <name>
show template-stack <name> config devices <name> network interface cluster-ethernet <name> layer3 proxy-arp
show template-stack <name> config devices <name> network interface cluster-ethernet <name> layer3 proxy-arp address
show template-stack <name> config devices <name> network interface cluster-ethernet <name> layer3 proxy-arp address <name>
show template-stack <name> config devices <name> network interface cluster-ethernet <name> layer3 units <name> proxy-arp
show template-stack <name> config devices <name> network interface cluster-ethernet <name> layer3 units <name> proxy-arp address
show template-stack <name> config devices <name> network interface cluster-ethernet <name> layer3 units <name> proxy-arp address <name>
show template-stack <name> config devices <name> network interface ethernet <name> layer2-switch
show template-stack <name> config devices <name> network interface ethernet <name> layer2-switch lldp
show template-stack <name> config devices <name> network interface ethernet <name> layer2-switch lldp high-availability
show template-stack <name> config devices <name> network interface ethernet <name> layer2-switch storm-control
show template-stack <name> config devices <name> network interface ethernet <name> layer2-switch storm-control broadcast
show template-stack <name> config devices <name> network interface ethernet <name> layer2-switch storm-control multicast
show template-stack <name> config devices <name> network interface ethernet <name> layer2-switch storm-control unicast
show template-stack <name> config devices <name> network interface ethernet <name> layer2-switch type
show template-stack <name> config devices <name> network interface ethernet <name> layer2-switch type access
show template-stack <name> config devices <name> network interface ethernet <name> layer2-switch type trunk
show template-stack <name> config devices <name> network interface ethernet <name> layer2-switch type trunk native
show template-stack <name> config devices <name> network interface ethernet <name> layer2-switch type trunk units
show template-stack <name> config devices <name> network interface ethernet <name> layer2-switch type trunk units <name>
show template-stack <name> config devices <name> network interface ethernet <name> layer3 proxy-arp
show template-stack <name> config devices <name> network interface ethernet <name> layer3 proxy-arp address
show template-stack <name> config devices <name> network interface ethernet <name> layer3 proxy-arp address <name>
show template-stack <name> config devices <name> network interface ethernet <name> layer3 units <name> proxy-arp
show template-stack <name> config devices <name> network interface ethernet <name> layer3 units <name> proxy-arp address
show template-stack <name> config devices <name> network interface ethernet <name> layer3 units <name> proxy-arp address <name>
show template-stack <name> config devices <name> network interface fail-open
show template-stack <name> config devices <name> network interface fail-open <name>
show template-stack <name> config devices <name> network interface vlan proxy-arp
show template-stack <name> config devices <name> network interface vlan proxy-arp address
show template-stack <name> config devices <name> network interface vlan proxy-arp address <name>
show template-stack <name> config devices <name> network interface vlan units <name> proxy-arp
show template-stack <name> config devices <name> network interface vlan units <name> proxy-arp address
show template-stack <name> config devices <name> network interface vlan units <name> proxy-arp address <name>
show template-stack <name> config devices <name> network profiles apn-profile
show template-stack <name> config devices <name> network profiles apn-profile <name>
show template-stack <name> config devices <name> network profiles stp-profile
show template-stack <name> config devices <name> network profiles stp-profile <name>
show template-stack <name> config devices <name> network profiles stp-profile <name> global-parameters
show template-stack <name> config devices <name> network profiles stp-profile <name> instance-parameters
show template-stack <name> config devices <name> network profiles stp-profile <name> port-parameters
show template-stack <name> config devices <name> network profiles stp-profile <name> port-parameters <name>
show template-stack <name> config devices <name> network routing-profile multicast mld-interface-query-profile
show template-stack <name> config devices <name> network routing-profile multicast mld-interface-query-profile <name>
show template-stack <name> config devices <name> network tunnel global-protect-gateway <name> ikev2
show template-stack <name> config devices <name> network tunnel global-protect-gateway <name> ikev2 Authentication
show template-stack <name> config devices <name> network tunnel global-protect-gateway <name> ikev2 Authentication certificate
show template-stack <name> config devices <name> network tunnel global-protect-gateway <name> ikev2 Authentication master-key
show template-stack <name> config devices <name> vsys <name> device-context-segment-association
show template-stack <name> config devices <name> vsys <name> device-context-segment-association <name>
show template-stack <name> config devices <name> vsys <name> global-protect global-protect-gateway <name> cloud-gp-svc
show template-stack <name> config devices <name> vsys <name> global-protect global-protect-gateway <name> cloud-gp-svc params
show template-stack <name> config devices <name> vsys <name> global-protect global-protect-gateway <name> cloud-gp-svc params <name>
show template-stack <name> config devices <name> vsys <name> global-protect global-protect-gateway <name> cloud-gp-svc params <name> value
show template-stack <name> config devices <name> vsys <name> global-protect global-protect-gateway <name> cloud-gp-svc params <name> value value-str-arr
show template-stack <name> config devices <name> vsys <name> global-protect global-protect-gateway <name> cloud-gp-svc params <name> value value-str-arr <name>
show template-stack <name> config devices <name> vsys <name> global-protect global-protect-gateway <name> cloud-gp-svc public-address
show template-stack <name> config devices <name> vsys <name> global-protect global-protect-portal <name> portal-config cloud-gp-svc
show template-stack <name> config devices <name> vsys <name> global-protect global-protect-portal <name> portal-config cloud-gp-svc params
show template-stack <name> config devices <name> vsys <name> global-protect global-protect-portal <name> portal-config cloud-gp-svc params <name>
show template-stack <name> config devices <name> vsys <name> global-protect global-protect-portal <name> portal-config cloud-gp-svc params <name> value
show template-stack <name> config devices <name> vsys <name> global-protect global-protect-portal <name> portal-config cloud-gp-svc params <name> value value-str-arr
show template-stack <name> config devices <name> vsys <name> global-protect global-protect-portal <name> portal-config cloud-gp-svc params <name> value value-str-arr <name>
show template-stack <name> config devices <name> vsys <name> setting session
show template-stack <name> config shared admin-role <name> role device webui objects mobile-network
show template-stack <name> config shared admin-role <name> role device webui objects panos-shield
show template-stack <name> config shared admin-role <name> role vsys webui objects mobile-network
show template-stack <name> config shared admin-role <name> role vsys webui objects panos-shield
show template-stack <name> config shared dlp-settings
show template-stack <name> config shared non-file-based-dlp-settings

 Previous 

 Load Commands Removed in PAN-OS 12.1 

 Next 

 Set Commands Introduced in PAN-OS 12.2 

 On This Page 

 Activation & Onboarding 

 Strata Cloud Manager 

 Activate a License or Product 

 Cloud Identity Engine 

 Strata Logging Service 

 Device Associations 

 Hub 

 Identity and Access Management 

 Tenant Management 

 Next-Generation Firewalls 

 AIOps for NGFW 

 Cloud Management for NGFWs 

 Cloud NGFW for AWS 

 Cloud NGFW for Azure 

 CN-Series 

 Firewalls 

 PAN-OS 

 PAN-OS SD-WAN 

 Service Provider 

 VM-Series 

 SASE 

 Prisma Access 

 Strata Multitenant Cloud Manager 

 AI-Powered ADEM 

 Prisma Access Monitoring and Visibility 

 Prisma SD-WAN 

 ION Devices 

 Next-Generation CASB 

 Cloud-Delivered Security Services 

 Advanced WildFire 

 Advanced URL Filtering 

 Advanced Threat Prevention 

 Advanced DNS Security 

 Device Security 

 Enterprise DLP 

 SaaS Security 

 Network Security 

 Shared Policy for NGFWs and Prisma Access 

 Visibility & Monitoring 

 Dashboards 

 Incidents and Alerts 

 Reports 

 Autonomous DEM 

 Best Practices 

 Best Practices Library 

 Experts Corner 

 Solutions Docs from Product Experts 

 CLI 

 PAN-OS 

 Next-Generation Firewall 

 Quick Start 

 © 2026 Palo Alto Networks, Inc. All rights reserved.
